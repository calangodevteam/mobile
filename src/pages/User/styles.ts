import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex:1,
  },
  containerView: {
    alignItems:'center',
  },
  userPerfil: {
    marginTop:40,
    alignItems:'center',
  },
  textPerfil : {
    marginVertical: 20,
    },
});
