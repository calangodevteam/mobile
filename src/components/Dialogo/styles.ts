import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderRadius:40,
  },
  title: {
    textAlign: 'center',
  },
  button: {
    paddingHorizontal: 10,
    marginTop:20,
    marginRight:10,
  },
});
