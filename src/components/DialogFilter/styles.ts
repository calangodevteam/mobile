import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderRadius:20,
    maxHeight:600,
  },
  scroll: {
    paddingHorizontal: 5,
  },
  viewContainer: {
    borderWidth:1,
    borderRadius:20,
    marginVertical:10,
  },
  viewRadio: {
    flexDirection:'row',
  },
  title: {
    textAlign: 'center',
  },
  button: {
    paddingHorizontal: 10,
    marginTop:20,
    marginRight:10,
  },
});
